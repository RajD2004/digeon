services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    environment:
      DB_HOST: database-1.c0zucywagdl4.us-east-1.rds.amazonaws.com
      DB_PORT: "3306"
      DB_NAME: main_db
      DB_MKT_NAME: marketplace_db
      DB_USER: admin
      DB_PASS: Row90bit_20041803-2022-2026
    volumes:
      - ./excel:/app/excel

  caddy:
    image: caddy:2
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - app

volumes:
  caddy_data:
  caddy_config:
